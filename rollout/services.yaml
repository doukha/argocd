apiVersion: v1
kind: Service
metadata:
  name: frontend-active
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: frontend

---
apiVersion: v1
kind: Service
metadata:
  name: frontend-preview
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: frontend
